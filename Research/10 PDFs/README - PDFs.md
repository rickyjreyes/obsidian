# PDFs

Place the actual paper PDFs in this folder, or provide a Zotero RDF export ZIP that includes its `files/` directory.

The current export contains metadata and links, but not the PDF files themselves.
